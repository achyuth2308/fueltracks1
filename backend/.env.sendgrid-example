# SendGrid Configuration
# Get your API key from app.sendgrid.com -> Settings -> API Keys
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# The email address verified in your SendGrid account (Sender Authentication)
FROM_EMAIL=alerts@fueltracks.in

# The display name that users will see in their inbox
FROM_NAME="FuelTracks Alerts"
